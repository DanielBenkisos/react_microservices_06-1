const Header = () => {
    return (
        <div>
            <div>Logo</div>
            <div>About</div>
            <a href="#">Login</a>
            <a href="#">Sign Up</a>
            <a href="#">Logout</a>
        </div>
    );
};

export default Header;